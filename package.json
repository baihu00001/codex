{
  "name": "weixin-sales",
  "version": "1.0.0",
  "description": "降价拍卖微信小程序",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js --ignore-pattern node_modules --ignore-pattern web"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
